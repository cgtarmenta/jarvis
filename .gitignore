/target
**/*.rs.bk
Cargo.lock.bak

# AUR build artifacts
packaging/pkg/
packaging/src/
packaging/*.tar.gz
packaging/*.tar.zst
packaging/*.pkg.tar*

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Spec-driven development — `specs/private/` is the per-developer scratch
# space for half-baked / experimental specs. Anything you want to share
# goes in `specs/inbox/` (still informal, but visible in PRs).
specs/private/
